# This repo is also a Zephyr module: it provides the rev57lp shield
# (board_root) and our own firmware features (cmake + kconfig).
build:
  cmake: zephyr
  kconfig: zephyr/Kconfig
  settings:
    board_root: .
